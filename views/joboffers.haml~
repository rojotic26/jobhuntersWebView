.container

  %form{action: '/joboffers', method: 'get', id: 'user-form'}

  %input{type: 'submit', id: 'home-form-submit', class: 'submit', value: 'submit'}
  %p

  -if @category
    %table{class: 'center'}
      %tbody
        %tr
          %th Job 
          %th Country
          %th Date
        -@jobofferobject['category'].each do |offer|
          %tr
            %td
              = offer['title']
            %td{width: '300'}
              = offer['city']
            %td{width: '100'}
              = offer['date']
